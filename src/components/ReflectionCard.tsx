import { reflectionForDate } from '../content/quotes'
export function ReflectionCard(){const item=reflectionForDate(),credit=item.author==='DeudARG'?'Reflexión del producto':`Idea inspirada en ${item.author}`;return <aside className="reflection-card"><span>{item.type==='quote'?'“':''}{item.text}{item.type==='quote'?'”':''}</span><small>{item.type==='quote'?`— ${item.author}${item.source?`, ${item.source}`:''}`:`${credit}${item.source?` · ${item.source}`:''}`}</small>{item.interpretation&&<p>{item.interpretation}</p>}</aside>}
