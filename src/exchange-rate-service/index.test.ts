import { describe,expect,it } from 'vitest'
import { convertUSDToARS,isCacheFresh,isRateStale,parseRateResponse,RATE_ENDPOINTS } from '.'
const raw={compra:1500,venta:1525,fechaActualizacion:'2026-09-01T12:00:00.000Z'}
describe('cotizaciones',()=>{it('convierte USD a ARS localmente',()=>expect(convertUSDToARS(50,1989)).toBe(99450));it('ARS no pasa por conversión',()=>expect(convertUSDToARS(0,1989)).toBe(0));it('diferencia fuente blue y tarjeta',()=>{expect(parseRateResponse('blue',raw).source).toBe('DolarHoy');expect(parseRateResponse('card',raw).source).toBe('Ámbito Financiero')});it.each([0,-1,Number.NaN])('rechaza venta inválida %s',venta=>expect(()=>parseRateResponse('blue',{...raw,venta})).toThrow());it('rechaza JSON inválido',()=>expect(()=>parseRateResponse('card',{})).toThrow());it('detecta caché fresca',()=>expect(isCacheFresh({...parseRateResponse('blue',raw),fetchedAt:new Date().toISOString()})).toBe(true));it('detecta cotización vieja',()=>expect(isRateStale(parseRateResponse('blue',{...raw,fechaActualizacion:'2020-01-01T00:00:00.000Z'}))).toBe(true));it('endpoints no contienen ningún dato financiero',()=>expect(Object.values(RATE_ENDPOINTS).every(url=>!url.includes('?'))).toBe(true))})
